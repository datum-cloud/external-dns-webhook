apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: route-parent-zone
  namespace: default
  annotations:
    external-dns.alpha.kubernetes.io/hostname: parent-app.example.com
spec:
  parentRefs:
  - name: default-gateway
    namespace: envoy-gateway-system
  hostnames:
  - parent-app.example.com
  rules:
  - backendRefs:
    - name: whoami
      port: 80
