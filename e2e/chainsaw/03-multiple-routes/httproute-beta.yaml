apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: multi-beta
  namespace: default
  annotations:
    external-dns.alpha.kubernetes.io/hostname: beta.example.com
spec:
  parentRefs:
  - name: default-gateway
    namespace: envoy-gateway-system
  hostnames:
  - beta.example.com
  rules:
  - backendRefs:
    - name: whoami
      port: 80
