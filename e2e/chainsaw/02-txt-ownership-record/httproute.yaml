apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: test-txt-httproute
  namespace: default
  annotations:
    external-dns.alpha.kubernetes.io/hostname: test-txt.example.com
spec:
  parentRefs:
  - name: default-gateway
    namespace: envoy-gateway-system
  hostnames:
  - test-txt.example.com
  rules:
  - backendRefs:
    - name: whoami
      port: 80
